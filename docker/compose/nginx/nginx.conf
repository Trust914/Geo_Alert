
### The below is the content of the nginx.conf file before we add the SSL configuration.

# events {
#     worker_connections 1024;
# }

# http {
#     server {
#         listen 80;
#         server_name api.geoalert.xyz;

#         # 1. This is the magic block that fixes the 404 error!
#         # It tells Nginx: "If Let's Encrypt asks for a challenge file, look in here."
#         location /.well-known/acme-challenge/ {
#             root /var/www/certbot;
#         }

#         # 2. Redirect everything else to HTTPS (we will configure HTTPS next)
#         location / {
#             return 301 https://$host$request_uri;
#         }
#     }
# }


### After adding the SSL configuration, the below is the updated nginx.conf file

events {
    worker_connections 1024;
}

http {
    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;

    server {
        listen 80;
        server_name api.geoalert.xyz;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name api.geoalert.xyz;

        ssl_certificate     /etc/letsencrypt/live/api.geoalert.xyz/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.geoalert.xyz/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-Content-Type-Options "nosniff";
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

        location / {
            limit_req zone=api burst=50 nodelay;

            proxy_pass http://geoalert-api:3000;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;

            proxy_connect_timeout 60s;
            proxy_send_timeout    60s;
            proxy_read_timeout    60s;
        }
    }
}